<% if admin_signed_in? %>
<div class="container">
  <div class="row">
    <div class="col-md-8 mx-auto mt-5">
      <h3><strong>会員情報詳細</strong></h3>
        <table class="table my-4">
          <tr>
            <th>会員ID</th>
            <td><%= @customer.id %></td>
          </tr>
          <tr>
            <th>ユーザーネーム</th>
            <td><%= @customer.name %></td>
          </tr>
          <tr>
            <th>メールアドレス</th>
            <td><%= @customer.email %></td>
          </tr>
          <tr>
            <th>会員ステータス</th>
            <td><%= @customer.is_deleted ? '退会' : '有効' %></td>
          </tr>
        </table>
      <%= link_to "一覧に戻る", admin_customers_path, class: "btn btn-outline-secondary" %></td>
  </div>
 </div>
</div>
<% else %>
  管理者専用ページです
<% end %>
